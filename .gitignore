.DS_Store
*.log
__pycache__/
.venv/

# _site/ is generated by build.py. Netlify rebuilds it on every deploy,
# so there's no reason to track it.
_site/

# Claude Code machine-local settings (permission grants for this Mac only)
.claude/settings.local.json
